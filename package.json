{
  "scripts": {
    "build": "asb --verbose",
    "deploy": "near dev-deploy --wasmFile=\"./contract.wasm\"",
    "reset": "rm -rf contract.wasm && rm -rf ./neardev"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "eslint": "^7.14.0",
    "near-cli": "^1.4.0",
    "near-sdk-as": "^2.1.0",
    "user-home": "^2.0.0"
  }
}
