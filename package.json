{
  "scripts": {
    "build": "asb --verbose",
    "deploy": "near dev-deploy --wasmFile=\"./contract.wasm\""
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "near-cli": "^1.4.0",
    "near-sdk-as": "^2.1.0"
  }
}
